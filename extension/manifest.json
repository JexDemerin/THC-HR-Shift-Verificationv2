{
  "manifest_version": 3,
  "name": "THC WellSky Shift Log",
  "version": "0.1.0",
  "description": "Mirrors WellSky shift records (caregiver, client, date, actual/scheduled clock times, status) into a Google Sheet.",
  "action": {
    "default_popup": "popup.html"
  },
  "background": {
    "service_worker": "background.js"
  },
  "commands": {
    "export-care-log-no-hover": {
      "suggested_key": { "default": "Ctrl+Shift+E" },
      "description": "Export Care Log HTML without moving the mouse (use while physically hovering a link)"
    }
  },
  "permissions": [
    "activeTab",
    "scripting",
    "downloads",
    "storage"
  ],
  "host_permissions": [
    "https://script.google.com/*",
    "https://script.googleusercontent.com/*"
  ]
}
